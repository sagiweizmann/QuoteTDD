version: v1.0
name: First pipeline example
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu1804
blocks:
  - name: "Build"
    task:
      env_vars:
        - name: APP_ENV
          value: prod
      jobs:
      - name: Docker build
        commands:
          - checkout
          - sudo apt-get install python3
          - echo "Installing dependenices"
          - sudo apt install python3-pip -y
  - name: "Unit tests"
    task:git
      jobs:
      - name: RSpec
        commands:
          - python3 -m pip install -r requirements.txt
          - python3 -m unittest tests/*.py
          - checkout
          - echo "Unit tests"
      - name: Lint code
        commands:
          - sudo apt install pycodestyle -y
          - cd /home/semaphore/QuoteTDD/
          - pycodestyle **/*.py
          - checkout
          - echo "make lint"

  - name: "Push Image"
    task:
      jobs:
      - name: Push
        commands:
          - checkout
          - echo "make docker.push"
